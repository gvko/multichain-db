version: '2'
services:
  ip-seeder:
    image: ip-seeder:latest
    ports:
      - '9009:3000'
    tty: true
    volumes:
      - './app:/app'
    command: 'npm run start-nodemon'
    tty: true
    container_name: ip-seeder
    networks:
      - default

networks:
  default:
    external:
      name: multichain-network
